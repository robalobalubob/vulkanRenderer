#include "vulkan-engine/audio/AudioListener.hpp"

namespace vkeng {

    // AudioListener is currently header-only in practice, but this .cpp
    // exists so the build system has a compilation unit for it. Future
    // backend integration will add implementation here.

} // namespace vkeng
